import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Header from "./ui/header";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "GitHub Repo Finder",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  // const bg = `bg-gradient-to-r from-indigo-500 from-10% via-sky-500 via-30% to-emerald-500 to-90%`;
  const bg = `antialiased text-slate-300 bg-slate-900`;
  return (
    <html lang="en">
      <body className={`${inter.className} m-7 bg-gradient-to-r ${bg} `}>
        <Header />
        {children}
      </body>
    </html>
  );
}
